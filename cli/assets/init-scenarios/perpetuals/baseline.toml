name = "baseline"
label = "Baseline smoke"
description = "Runs the perpetuals adapter with normal price noise and no protocol perturbation; useful as a wiring smoke test."
scenario = "baseline"
agents = 50
ticks = 60
recommended = true
required = true
