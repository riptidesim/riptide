A previous attempt produced output that failed validation. The raw
output is quoted below between the `===PREVIOUS===` fences and the
specific validation error is quoted below that between the `===ERROR===`
fences.

Do not apologize. Do not explain. Re-emit a corrected TOML that would
pass validation. Fix the specific error reported. Keep everything that
was already valid. Obey every rule from the original system prompt,
especially "output only TOML, no prose, no markdown fences".

===PREVIOUS===
{{previous}}
===PREVIOUS===

===ERROR===
{{error}}
===ERROR===
